const initialState = {
    ingredients: {
        lactic: 5,
        strawbarry: 5,
        chocolate: 5,
        bananas: 5,
        pistachio: 5,
        
      },
      price: 200,
};

const builderReducer = (state = initialState, action) => {
  return state;
}

export default builderReducer;